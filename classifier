#!/usr/bin/env python

from lib import *


def main() -> None:
    print(f"Accuracy: {100 * score_classifier()}%")
    # classifier = TextClassifier.from_cached()
    # print(classifier.predict("2k hours, looking for a squad to raid with"))
    # print(classifier.predict("Difficulty figuring out how to implement Iterator on custom types"))


if __name__ == "__main__":
    main()
